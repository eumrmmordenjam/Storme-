<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>STORME — Web Security</title>

<!-- Google Fonts modernas -->
<link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&family=Montserrat:wght@300;600&display=swap" rel="stylesheet">

<!-- AOS para animação ao rolar -->
<link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css"/>

<style>
:root{
  --bg:#070607;
  --panel: rgba(16,16,18,0.86);
  --neon:#8b6cff;
  --neon-2:#4de0c6;
  --muted:#bfbfbf;
  --accent:#bb86fc;
}

*{box-sizing:border-box; margin:0; padding:0;}

body {
  font-family: 'Montserrat', sans-serif;
  background:#070607;
  color:#e6e6e6;
  min-height:100vh;
  display:flex;
  flex-direction:column;
  scroll-behavior: smooth; /* smooth scroll global */
}

/* Hero */
.hero {
  position:relative;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  text-align:center;
  padding:80px 20px;
  background: linear-gradient(180deg, rgba(10,10,12,0.95), rgba(8,8,10,0.95)), url('https://images.unsplash.com/photo-1527443224154-9f3e0b15f1d2?auto=format&fit=crop&w=1400&q=80') no-repeat center center;
  background-size:cover;
}

.hero h1 {
  font-family: 'Share Tech Mono', monospace;
  font-size: 3rem;
  color: var(--accent);
  text-shadow: 0 0 20px rgba(187,134,252,0.5);
  margin-bottom:10px;
}

.hero p {
  font-size:1.2rem;
  color: var(--muted);
  max-width:700px;
  margin-bottom:20px;
}

/* Services */
.services {
  display:grid;
  grid-template-columns: repeat(auto-fit,minmax(250px,1fr));
  gap:20px;
  padding:60px 20px;
  max-width:1200px;
  margin:0 auto;
}

.card {
  background: var(--panel);
  padding:24px;
  border-radius:12px;
  box-shadow:0 10px 40px rgba(0,0,0,0.6);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 60px rgba(77,224,198,0.4);
}

.card h3 {
  color: #fff;
  margin-bottom:12px;
  font-size:1.2rem;
}

.card p {
  color: #ccc;
  font-size:0.95rem;
  line-height:1.4;
}

/* CTA buttons */
.btn {
  display:inline-block;
  padding:12px 20px;
  border-radius:10px;
  font-weight:600;
  text-decoration:none;
  color:#071012;
  background: linear-gradient(90deg,var(--neon),var(--neon-2));
  box-shadow:0 8px 30px rgba(77,224,198,0.2);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.btn:hover {
  transform: translateY(-3px);
  box-shadow:0 14px 50px rgba(77,224,198,0.35);
}

/* Footer */
footer {
  padding:20px;
  text-align:center;
  font-size:0.85rem;
  color:var(--muted);
  border-top:1px solid rgba(255,255,255,0.05);
}

/* Particles canvas */
#particles {
  position:fixed;
  top:0;
  left:0;
  width:100%;
  height:100%;
  z-index:-1;
}

/* Fade-up animation */
[data-aos] {opacity:0; transform: translateY(20px);}
</style>
</head>
<body>

<canvas id="particles"></canvas>

<header class="hero" data-aos="fade-up">
  <h1>STORME</h1>
  <p>Protegendo o caos da web desde 2025 — auditoria de vulnerabilidades, proteção DDoS e desenvolvimento seguro.</p>
  <a href="#services" class="btn" data-aos="fade-up">Ver Serviços</a>
</header>

<main class="services" id="services">
  <div class="card" data-aos="fade-up">
    <h3>Auditoria & Pentest</h3>
    <p>Testes manuais e automáticos para descobrir falhas antes dos invasores.</p>
  </div>
  <div class="card" data-aos="fade-up" data-aos-delay="100">
    <h3>Proteção DDoS</h3>
    <p>Mitigação ativa, regras e filtros para manter seu site no ar sob ataque.</p>
  </div>
  <div class="card" data-aos="fade-up" data-aos-delay="200">
    <h3>Firewall & WAF</h3>
    <p>Regras personalizadas e bloqueios inteligentes contra tráfego malicioso.</p>
  </div>
  <div class="card" data-aos="fade-up" data-aos-delay="300">
    <h3>Desenvolvimento Seguro</h3>
    <p>Código limpo, CI/CD com scans e correções na fonte.</p>
  </div>
</main>

<footer>
  © 2025 STORME — Web Security & Dark Design
</footer>

<!-- Scripts -->
<script src="https://unpkg.com/aos@next/dist/aos.js"></script>
<script>
  AOS.init({ duration:1000, once:true });

  // Smooth scroll para links internos
  document.querySelectorAll('a[href^="#"]').forEach(anchor=>{
    anchor.addEventListener('click', function(e){
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if(target){
        target.scrollIntoView({behavior:'smooth'});
      }
    });
  });

  // Partículas neon flutuantes
  const canvas = document.getElementById('particles');
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;

  let particlesArray = [];
  const colors = ["#5500ff","#bb86fc","#4de0c6"];
  class Particle {
    constructor(){
      this.x = Math.random()*canvas.width;
      this.y = Math.random()*canvas.height;
      this.size = Math.random()*2+1;
      this.speedY = Math.random()*0.5+0.2;
      this.color = colors[Math.floor(Math.random()*colors.length)];
    }
    update(){ this.y -= this.speedY; if(this.y<0) this.y=canvas.height; }
    draw(){
      ctx.fillStyle=this.color;
      ctx.beginPath();
      ctx.arc(this.x,this.y,this.size,0,Math.PI*2);
      ctx.fill();
    }
  }
  function init(){ particlesArray=[]; for(let i=0;i<120;i++){particlesArray.push(new Particle()); } }
  function animate(){
    ctx.clearRect(0,0,canvas.width,canvas.height);
    particlesArray.forEach(p=>{ p.update(); p.draw(); });
    requestAnimationFrame(animate);
  }
  init(); animate();
  window.addEventListener('resize',()=>{ canvas.width=window.innerWidth; canvas.height=window.innerHeight; init(); });
</script>
</body>
</html><!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>STORME — Web Security</title>

<!-- Google Fonts modernas -->
<link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&family=Montserrat:wght@300;600&display=swap" rel="stylesheet">

<!-- AOS para animação ao rolar -->
<link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css"/>

<style>
:root{
  --bg:#070607;
  --panel: rgba(16,16,18,0.86);
  --neon:#8b6cff;
  --neon-2:#4de0c6;
  --muted:#bfbfbf;
  --accent:#bb86fc;
}

*{box-sizing:border-box; margin:0; padding:0;}

body {
  font-family: 'Montserrat', sans-serif;
  background:#070607;
  color:#e6e6e6;
  min-height:100vh;
  display:flex;
  flex-direction:column;
}

/* Hero */
.hero {
  position:relative;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  text-align:center;
  padding:80px 20px;
  background: linear-gradient(180deg, rgba(10,10,12,0.95), rgba(8,8,10,0.95)), url('https://images.unsplash.com/photo-1527443224154-9f3e0b15f1d2?auto=format&fit=crop&w=1400&q=80') no-repeat center center;
  background-size:cover;
}

.hero h1 {
  font-family: 'Share Tech Mono', monospace;
  font-size: 3rem;
  color: var(--accent);
  text-shadow: 0 0 20px rgba(187,134,252,0.5);
  margin-bottom:10px;
}

.hero p {
  font-size:1.2rem;
  color: var(--muted);
  max-width:700px;
}

/* Services */
.services {
  display:grid;
  grid-template-columns: repeat(auto-fit,minmax(250px,1fr));
  gap:20px;
  padding:60px 20px;
  max-width:1200px;
  margin:0 auto;
}

.card {
  background: var(--panel);
  padding:24px;
  border-radius:12px;
  box-shadow:0 10px 40px rgba(0,0,0,0.6);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 60px rgba(77,224,198,0.4);
}

.card h3 {
  color: #fff;
  margin-bottom:12px;
  font-size:1.2rem;
}

.card p {
  color: #ccc;
  font-size:0.95rem;
  line-height:1.4;
}

/* CTA buttons */
.btn {
  display:inline-block;
  padding:12px 20px;
  border-radius:10px;
  font-weight:600;
  text-decoration:none;
  color:#071012;
  background: linear-gradient(90deg,var(--neon),var(--neon-2));
  box-shadow:0 8px 30px rgba(77,224,198,0.2);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.btn:hover {
  transform: translateY(-3px);
  box-shadow:0 14px 50px rgba(77,224,198,0.35);
}

/* Footer */
footer {
  padding:20px;
  text-align:center;
  font-size:0.85rem;
  color:var(--muted);
  border-top:1px solid rgba(255,255,255,0.05);
}

/* Particles canvas */
#particles {
  position:fixed;
  top:0;
  left:0;
  width:100%;
  height:100%;
  z-index:-1;
}

/* Fade-up animation */
[data-aos] {opacity:0; transform: translateY(20px);}
</style>
</head>
<body>

<canvas id="particles"></canvas>

<section class="hero" data-aos="fade-up">
  <h1>STORME</h1>
  <p>Protegendo o caos da web desde 2025 — auditoria de vulnerabilidades, proteção DDoS e desenvolvimento seguro.</p>
  <a href="#services" class="btn" data-aos="fade-up">Ver Serviços</a>
</section>

<section class="services" id="services">
  <div class="card" data-aos="fade-up">
    <h3>Auditoria & Pentest</h3>
    <p>Testes manuais e automáticos para descobrir falhas antes dos invasores.</p>
  </div>
  <div class="card" data-aos="fade-up" data-aos-delay="100">
    <h3>Proteção DDoS</h3>
    <p>Mitigação ativa, regras e filtros para manter seu site no ar sob ataque.</p>
  </div>
  <div class="card" data-aos="fade-up" data-aos-delay="200">
    <h3>Firewall & WAF</h3>
    <p>Regras personalizadas e bloqueios inteligentes contra tráfego malicioso.</p>
  </div>
  <div class="card" data-aos="fade-up" data-aos-delay="300">
    <h3>Desenvolvimento Seguro</h3>
    <p>Código limpo, CI/CD com scans e correções na fonte.</p>
  </div>
</section>

<footer>
  © 2025 STORME — Web Security & Dark Design
</footer>

<!-- AOS Script -->
<script src="https://unpkg.com/aos@next/dist/aos.js"></script>
<script>
  AOS.init({
    duration: 1000,
    once:true
  });

  // Partículas neon flutuantes
  const canvas = document.getElementById('particles');
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;

  let particlesArray = [];
  const colors = ["#5500ff","#bb86fc","#4de0c6"];
  class Particle {
    constructor(){
      this.x = Math.random()*canvas.width;
      this.y = Math.random()*canvas.height;
      this.size = Math.random()*2+1;
      this.speedY = Math.random()*0.5+0.2;
      this.color = colors[Math.floor(Math.random()*colors.length)];
    }
    update(){ this.y -= this.speedY; if(this.y<0) this.y=canvas.height; }
    draw(){
      ctx.fillStyle=this.color;
      ctx.beginPath();
      ctx.arc(this.x,this.y,this.size,0,Math.PI*2);
      ctx.fill();
    }
  }
  function init(){ particlesArray=[]; for(let i=0;i<120;i++){particlesArray.push(new Particle()); } }
  function animate(){
    ctx.clearRect(0,0,canvas.width,canvas.height);
    particlesArray.forEach(p=>{ p.update(); p.draw(); });
    requestAnimationFrame(animate);
  }
  init(); animate();
  window.addEventListener('resize',()=>{ canvas.width=window.innerWidth; canvas.height=window.innerHeight; init(); });
</script>

</body>
</html>